{#
	The look of an option table, shared by the network and the radio page.
#}
<style>
	/* Everything that was here and is used by more than the two editors moved
	   to _components.html.twig — the card, the pills, the tags, the toolbar,
	   the group boxes and the option table. What is left is the editor's own. */
	.toolbar { position: sticky; top: 0; z-index: 5; background: var(--bg); padding: .5rem 0;
	           margin: 0; border-bottom: 1px solid var(--line); }
</style>
